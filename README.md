# ТЗ на Telegram‑бота kick

## 1. Цель и общая идея

Бот — это нейтральный инструмент для регулярной практики осознавания.По сути своей интеллектуальный таймер, который присылает сообщения по времени и может задавать тематические вопросы.
Цель - структурированная практика (по ДЗ) — несколько раз в день проходить через одинаковую последовательность шагов осознавания.

Бот:
- не оценивает,
- не утешает,
- не даёт советов,
- не интерпретирует,
- только задаёт вопросы, фиксирует и показывает накопленное.

Записи сохраняются в базу данных отдельно для каждого пользователя с полями "дата (date)", "содержание (context)", "отношение (attitude)", "форма согласия (form)", "реакция тела (body)", "изменения (response)". Собственно каждое поле - это каждый вопрос и плюс дата.

---

## 2. Основной функционал

### 2.1. Напоминания

- Пользователь может настроить напоминания в свободной форме:
  - «Напоминай мне с 9 до 22 каждые 2 часа»
  - «В будни в 10:00, 13:00, 17:00 и 21:00»
  - «Напоминай 4 раза в день»
- GPT парсит запрос пользователя в конкретные времена (принимает решения за пользователя, если запрос неточный)
- В указанные моменты бот присылает короткое сообщение:
  - «Готов записать наблюдение?» с кнопками:
    1. **"Да, пошагово"** - начать пошаговую практику осознавания
    2. **"Нет"** - отклонить (сохраняется отказ в БД для аналитики)

#### Пошаговая практика (после нажатия "Да, пошагово"):

Бот последовательно задаёт 5 вопросов (полей):

1. **Содержание**: Что в поле внимания (внутреннее или внешнее)?
2. **Отношение**: Баланс напряжения и расслабления в теле?
3. **Форма согласия**: Выражение отношения (Да-принимающее, Нет-принимающее, Да-отрицающее, Нет-отрицающее)?
4. **Реакция тела**: Осознавание совпадает с реакцией тела?
5. **Изменения**: Что происходит после осознавания?

**Процесс сбора каждого поля:**

1. Бот задаёт вопрос
2. Пользователь отправляет ответ (текст или голос)
3. Бот показывает ответ с кнопками и пояснением - подтвердите ответ или добавьте что-то если необходимо:
   - **"Всё ок"** - подтвердить ответ и перейти к следующему полю
4. Если пользователь вводит новый текст:
   - Бот добавляет его к предыдущему ответу
   - Снова показывает кнопки "Всё ок" / "Уточнить/добавить"
   - **Неограниченное количество попыток уточнения**
5. Когда пользователь нажимает "Всё ок":
   - Ответ сохраняется
   - Бот переходит к следующему полю
6. После последнего поля запись автоматически сохраняется в БД

**Преимущества пошагового подхода:**
- Чёткая структура данных (каждое поле явно заполнено)
- Пользователь контролирует полноту каждого ответа
- Не нужен GPT для парсинга свободного текста
- Простая логика состояний

### 2.2. Обработка голосовых сообщений

- Пользователь может отправлять голосовые сообщения в любой момент практики
- Бот использует OpenAI Whisper для транскрибации
- Бот показывает распознанный текст пользователю для проверки
- После проверки текст обрабатывается так же, как обычное текстовое сообщение

### 2.3. Формат сохранения

Каждая запись структурированной практики сохраняется в БД с полями:

- **date** (timestamp) - дата и время записи
- **content** - содержание (что в поле внимания)
- **attitude** - отношение (баланс напряжения/расслабления)
- **form** - форма согласия (да/нет принимающее/отрицающее)
- **body** - реакция тела (совпадает ли осознавание)
- **response** - изменения (что происходит после)

## 3. Работа c накопленными записями

Бот умеет экспортировать историю записей по запросу пользователя.

### Команды экспорта:
- `/export` или «Покажи результаты» - бот предлагает выбрать формат:
  - **JSON** - структурированный формат для программной обработки
  - **TXT** - текстовый формат для чтения

Бот формирует файл и отправляет его в чат.

### Формат TXT выдачи:

```
2025-11-20 10:15
Содержание: Тревога по поводу встречи
Отношение: Напряжение в груди и плечах
Форма согласия: Нет-отвергающее
Реакция тела: Да, совпадает
Изменения: Напряжение немного уменьшилось

2025-11-20 14:30
Содержание: ...
Отношение: ...
Форма согласия: ...
Реакция тела: ...
Изменения: ...
```

### Формат JSON выдачи:

```json
[
  {
    "date": "2025-11-20 10:15:00",
    "content": "Тревога по поводу встречи",
    "attitude": "Напряжение в груди и плечах",
    "form": "Нет-отвергающее",
    "body": "Напряжение в поянице. Сжало живот.",
    "response": "Напряжение немного уменьшилось"
  },
  ...
]
```

### Очистка истории:
- `/clear_history` - полное удаление всех записей пользователя из БД
- Требуется подтверждение (предупреждение о невозможности восстановления)

---

## 4. Стиль общения бота

- Максимально нейтрально.
- При суммаризации и подытоживании опираться только на слова пользователя.
- Без смайликов, без мотивации, без оценок и советов.
- Примеры допустимых фраз:
  - «Готов записать наблюдение?»
  - «Отправь голосовое сообщение или напиши текст.»
  - «Содержание: что в поле внимания?»
  - «Запись сохранена.»
  - «Напоминания настроены.»

---

## 5. Техническая реализация

### 5.1. Архитектура

```
src/
  main.py              # Точка входа, регистрация обработчиков
  config.py            # Конфигурация, фразы бота, константы
  database.py          # Работа с SQLite БД
  handlers/
    auth.py            # Аутентификация по паролю
    reminders.py       # Настройка расписания, обработка ответов на напоминания
    practice.py        # Пошаговая практика с неограниченными уточнениями
    export.py          # Экспорт данных (TODO)
  services/
    scheduler.py       # APScheduler для напоминаний
    schedule_parser.py # GPT-парсинг расписаний
    transcription.py   # Whisper транскрибация (TODO)
    openai_client.py   # Клиент OpenAI API (TODO)
```

### 5.2. Технологический стек

- **python-telegram-bot** 20.7 - Telegram Bot API
- **openai** 1.12.0 - OpenAI API (GPT для парсинга, Whisper для транскрибации)
- **apscheduler** 3.10.4 - Планировщик напоминаний
- **sqlalchemy** 2.0.23 - ORM для персистентного хранения задач планировщика
- **python-dotenv** 1.0.0 - Загрузка переменных окружения
- **pytz** 2024.1 - Поддержка часовых поясов

### 5.3. База данных (SQLite)

**Таблица `users`:**
- `telegram_user_id` (PRIMARY KEY) - ID пользователя Telegram
- `is_authenticated` - флаг аутентификации
- `first_auth_date` - дата первой аутентификации
- `last_active` - последняя активность
- `reminder_schedule` - JSON с распарсенным расписанием

**Таблица `entries`:**
- `id` (AUTOINCREMENT PRIMARY KEY)
- `telegram_user_id` (FOREIGN KEY)
- `date` - дата и время записи
- `content` - содержание (обязательное)
- `attitude` - отношение (обязательное)
- `form` - форма согласия (обязательное)
- `body` - реакция тела (обязательное)
- `response` - изменения (обязательное)

**Таблица `refusals`:**
- `id` (AUTOINCREMENT PRIMARY KEY)
- `telegram_user_id` (FOREIGN KEY)
- `date` - дата отказа от практики

### 5.4. Планировщик напоминаний

- **APScheduler** с SQLite job store для персистентности
- Задачи сохраняются в `src/data/scheduler.db`
- Автоматическое восстановление задач после перезапуска бота
- Поддержка фильтров дней: `weekdays`, `weekends`, `all`
- Поддержка часовых поясов (по умолчанию Europe/Moscow)

### 5.5. GPT парсинг расписания

- Пользователь отправляет запрос в свободной форме
- GPT интерпретирует неточные запросы ("утром", "часто") и принимает решения
- Возвращает JSON:
  ```json
  {
    "times": ["09:00", "13:00", "17:00", "21:00"],
    "day_filter": "weekdays",
    "timezone": "Europe/Moscow"
  }
  ```
- Никогда не запрашивает уточнений - всегда делает разумные предположения

### 5.6. Управление состоянием

Используется `context.user_data` для хранения состояния сессии:

```python
{
    'authenticated': True,
    'mode': 'guided_practice',
    'current_field': 2,  # Индекс текущего поля (0-4)
    'practice_data': {
        'content': '...',
        'attitude': '...'
    },
    'current_answer': 'Текущий ответ, который уточняется'
}
```

### 5.7. Аутентификация

- Единый пароль для всех пользователей (из `.env`)
- Проверка при `/start`
- Сохранение состояния аутентификации в БД
- Персистентность между перезапусками

### 5.8. Мультипользовательская поддержка

- Каждый пользователь полностью изолирован по `telegram_user_id`
- Независимые расписания напоминаний
- Независимые сессии практики
- Независимые записи в БД
- Рассчитано на ~20 одновременных пользователей

---

## 6. Текущий статус реализации

### ✅ Реализовано:
- База данных (SQLite) с изоляцией пользователей
- Аутентификация по паролю
- Планировщик напоминаний (APScheduler + SQLite job store)
- GPT-парсинг расписаний
- Обработчик напоминаний (2 кнопки: "Да, пошагово" / "Нет")
- **Пошаговая практика - ПОЛНОСТЬЮ РЕАЛИЗОВАНА:**
  - Навигация по 5 полям (content, attitude, form, body, response)
  - Аккумулятор сообщений (0.5с буферизация) для каждого поля
  - Кнопка "Назад" для возврата к предыдущим полям
  - Сохранение предыдущих ответов с возможностью замены
  - Валидация полей (запрет пустых ответов)
  - Автоматическая очистка таймеров при навигации и завершении
- Сохранение структурированных записей в БД
- Сервис транскрибации голоса (Whisper)

### 🚧 В разработке:
- Интеграция голосовых сообщений в режим практики
- Экспорт данных (JSON/TXT)
- Очистка истории
- Полное end-to-end тестирование

### 📋 Документация:
- `CLAUDE.md` - Инструкции для Claude Code
- `PLAN.md` - План разработки по фазам
- `SCHEDULER_INTEGRATION.md` - Руководство по планировщику
- `REMINDER_HANDLER_INTEGRATION.md` - Руководство по обработчикам напоминаний и практики

---
